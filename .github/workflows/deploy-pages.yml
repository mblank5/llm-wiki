name: Deploy Wiki to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install deps
        run: pip install markdown pyyaml

      - name: Lint wiki content
        run: python3 scripts/lint-wiki.py . --max-findings 80

      - name: Generate static site
        run: python3 scripts/generate-web.py

      - name: Validate generated search index
        run: python3 -m json.tool web/output/search-index.json > /dev/null

      - name: Create symlinks as copies for Pages
        run: |
          cd web/output/source
          for link in entities concepts queries raw papers paper-briefs references; do
            if [ -L "$link" ]; then
              target=$(readlink -f "$link")
              rm "$link"
              cp -r "$target" "$link"
            fi
          done

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: web/output

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
